# История изменений проекта

Этот файл является обязательным для чтения перед началом каждой рабочей сессии вместе с `AGENTS.md`, `project-requirements.md` и `task.md`. В нем фиксируются выполненные этапы, ключевые решения, проверки и важные ограничения, чтобы `AGENTS.md` оставался компактным файлом правил.

## История изменений

2026-06-12: Сформировано подробное техническое задание для MVP справочного сервиса официальных ответов городской администрации. Документ сохранен в `project-requirements.md`. Зафиксированы ключевые решения: Python/FastAPI, Jinja2/HTMX, SQLite с возможностью перехода на PostgreSQL, aiogram для Telegram-бота, локальный гибридный поиск без внешних AI API, запуск через `docker compose up`, один администратор через переменные окружения.
2026-06-12: `project-requirements.md` добавлен в раздел обязательных файлов для чтения перед началом работы.
2026-06-12: Добавлен чеклист после создания или изменения важных документов, чтобы новые опорные документы сразу попадали в обязательные файлы для чтения и историю изменений.
2026-06-12: Уточнены границы MVP в `project-requirements.md`: импорт поддерживает стандартную JSON-выгрузку Telegram без HTML, реклама ограничена флагом `ADS_ENABLED=false` без рекламной админки и блоков, роли и суперадмин не реализуются, юридические документы пишутся как рабочие финальные тексты проекта, автоматическое обезличивание закрывает очевидные персональные данные, а спорные случаи отправляются на ручную проверку.
2026-06-12: В `project-requirements.md` добавлено правило обезличивания: официальные телефоны, адреса, email, сайты и названия ведомств/организаций, нужные жителю для обращения за помощью, не скрываются как персональные данные; спорные контакты отправляются на ручную проверку.
2026-06-12: В `project-requirements.md` добавлена публично активная тема MVP "Животные" вместе с ЖКХ. Зафиксированы базовые категории: безнадзорные собаки, отлов безнадзорных животных, агрессивные животные, приюты и передержка, правила содержания животных. Решение позднее пересмотрено 2026-06-16: животные категории перенесены внутрь блока ЖКХ.
2026-06-12: В `project-requirements.md` добавлена правовая база для юридических документов: Конституция РФ ст. 29, 149-ФЗ, 8-ФЗ, 59-ФЗ, 152-ФЗ. Для описания сервиса выбрана формулировка "информационно-поисковый сервис справочного характера", чтобы подчеркнуть функцию поиска официально опубликованных материалов и не создавать впечатление официального ресурса или канала приема обращений.
2026-06-12: В `project-requirements.md` уточнены пользовательские формулировки выдачи: вместо обещающих фраз "по вашему вопросу" и "по вашей теме" используются более аккуратные "на похожий запрос" и "по смысловому сходству с вашим запросом".
2026-06-12: В `project-requirements.md` уточнено, что отсутствие пользовательского списка категорий/слов-маркеров, домена, финального названия и реквизитов юридических документов не блокирует разработку. Для старта используются базовые словари из ТЗ, рабочее название "Городской справочник", `PUBLIC_BASE_URL=http://localhost:8000` и явные юридические заглушки, которые обязательно заменяются на финальном этапе перед публичным запуском.
2026-06-12: В `project-requirements.md` уточнен процесс пополнения словарей поиска: используется базовый словарь из ТЗ и локальная морфологическая нормализация русского языка, система предлагает кандидаты из импортов и проблемных запросов, администратор только подтверждает, отклоняет или привязывает кандидаты; автоматическое добавление новых маркеров в публичный поиск без подтверждения запрещено.
2026-06-12: Создан `task.md` с 10 автономными этапами реализации MVP на основе `project-requirements.md`. `task.md` добавлен в обязательные файлы для чтения. Добавлено правило читать `project-requirements.md` и `task.md` в каждой сессии до полной реализации и приемки всех задач из `task.md`.
2026-06-15: Реализована задача 1: добавлен базовый FastAPI-каркас в `app/`, минимальные публичная страница, `/health` и заглушка `/admin`, конфигурация окружения, логирование, отключение Telegram-бота с понятным логом при пустом `TELEGRAM_BOT_TOKEN`, Dockerfile, `docker-compose.yml`, `.env.example`, `.gitignore`, `requirements.txt`, минимальная заготовка Alembic и smoke-тесты в `tests/`. Запуск задокументирован в `README.md`: основная команда `docker compose up`, тесты `docker compose run --rm app pytest`. В текущем PowerShell команды `docker`, `python` и `py` не найдены, поэтому Docker-проверка требует установки Docker или добавления его в PATH; локальная проверка возможна через bundled Python Codex.
2026-06-15: Для задачи 1 добавлен `.dockerignore`, а правило `.gitignore` для JSON сужено до приватных папок `imports/` и `exports/`, чтобы будущие тестовые JSON-fixtures можно было хранить в репозитории. Повторно проверены smoke-тесты через bundled Python Codex: 5 тестов проходят. Попытка установить Docker Desktop через Chocolatey зарегистрировала пакет `docker-desktop 4.77.0`, но `docker.exe` не появился в PATH; повышенный процесс установки завис на скачивании MSI и не может быть завершен из текущей сессии из-за прав доступа. Для приемочной Docker-проверки нужно завершить зависший установщик от имени администратора или перезапустить Windows, затем повторить установку Docker Desktop и выполнить `docker compose up`.
2026-06-15: После закрытия зависшего установщика повторная установка Docker Desktop через Chocolatey завершилась неуспешно из-за сетевого обрыва загрузки MSI с `desktop.docker.com`: `Received an unexpected EOF or 0 bytes from the transport stream`. Прямые попытки докачки через `curl` и BITS также обрываются; `docker.exe` отсутствует. Дополнительно проверен прямой запуск FastAPI через bundled Python: `/`, `/health` и `/admin` возвращают HTTP 200. Кодовая часть задачи 1 завершена, Docker-проверка требует ручной установки Docker Desktop из полностью скачанного MSI/installer, затем запуска `docker compose up`.
2026-06-16: Docker Desktop установлен вручную и запущен. Для поддержки запуска из папки с кириллицей в `docker-compose.yml` добавлено явное имя проекта `name: gorodskoy`; добавлен `pytest.ini` с `pythonpath = .`, чтобы тесты стабильно импортировали пакет `app` внутри контейнера. Приемка задачи 1 через Docker выполнена: `docker compose up -d --build` поднимает приложение, `/`, `/health` и `/admin` возвращают HTTP 200, в логах есть сообщение `TELEGRAM_BOT_TOKEN is not set; Telegram bot is disabled.`, команда `docker compose run --rm app pytest` проходит: 5 тестов.
2026-06-16: В `task.md` добавлена новая задача 2 "Безопасный перенос проекта в путь с латиницей". Причина: кириллический путь уже вызвал проблему с именем проекта Docker Compose и может повторно мешать внешним CLI-инструментам. Решение принято не переименовывать активную рабочую папку "наживую", а выполнить перенос отдельным проверяемым этапом с сохранением старой папки до успешной приемки новой.
2026-06-16: Реализована задача 2: рабочая копия проекта перенесена в ASCII-путь `C:\Projects\gorodskoy`, старая папка `C:\Users\user-pc\Desktop\Нейро\Городской` оставлена как резерв до ручной приемки. Технические файлы переименованы: `Agents.md` -> `AGENTS.md`, `Бриф.txt` -> `brief.txt`; ссылки в документации обновлены. Новая копия создается без кэшей `__pycache__`, `.pytest_cache`, `*.pyc` и без приватного содержимого `data/`, `imports/`, `exports/`, `logs`, кроме `.gitkeep`. Проверки из нового пути: bundled Python pytest, Docker Compose запуск, HTTP smoke `/`, `/health`, `/admin`, лог отключенного Telegram-бота и `docker compose run --rm app pytest`.
2026-06-16: Реализована задача 3: добавлена полная MVP-схема БД на SQLAlchemy 2.x для сущностей `materials`, `sources`, `topics`, `categories`, `resident_questions`, `question_variants`, `material_links`, `import_batches`, `import_reports`, `redaction_events`, `person_name_reviews`, `problem_queries`, `admin_notes`, `settings`; создана первая Alembic-миграция `20260616_0001_create_mvp_schema`; добавлены enum-статусы, DB session/engine с включенными SQLite foreign keys, seed-данные тем/категорий и `ADS_ENABLED=false`, а также repository-слой в `app/db/repositories.py`. Зафиксировано решение не добавлять SQLite FTS в задаче 3: полнотекстовый поиск остается отдельным модулем задачи 8; приватные JSON-исходники хранятся файлами, а БД хранит безопасные метаданные, отчеты и пути. Исправлена конфигурация логирования и Alembic `fileConfig`, чтобы тестовый перехват логов не ломался. Проверки: bundled Python pytest - 10 тестов проходят; `docker compose build app`; `docker compose run --rm app pytest` - 10 тестов проходят.
2026-06-16: В `README.md` добавлены команды автозапуска Windows из актуальной рабочей папки `C:\Projects\gorodskoy`: запуск `C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-Location -LiteralPath 'C:\Projects\gorodskoy'; docker compose up -d --build"` и остановка `C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-Location -LiteralPath 'C:\Projects\gorodskoy'; docker compose down"`. Команды рассчитаны на установленный и запущенный Docker Desktop.
2026-06-16: Для устранения повторяющейся проблемы чтения русскоязычных обязательных документов в Windows PowerShell 5.1 файлы `project-requirements.md` и `task.md` сохранены как UTF-8 с BOM, как и `AGENTS.md`. Это позволяет обычному `Get-Content` показывать русский текст корректно без явного `-Encoding UTF8`; содержимое документов не изменялось.
2026-06-16: Реализована задача 4: инлайн HTML главной страницы заменен публичным роутером `app/public/routes.py` и Jinja2-шаблонами `app/templates/public/`; добавлены главная, временная страница поиска, публичная карточка материала, подтверждение "Ответ не подошел" и 6 юридических страниц в `app/templates/legal/` с рабочими MVP-текстами и явными заглушками реквизитов. Добавлен спокойный справочный CSS в `app/static/styles.css`. Добавлен локальный DB bootstrap `app/services/bootstrap.py`: при `AUTO_DB_BOOTSTRAP=true` на старте применяются Alembic-миграции до head и idempotent seed тем/категорий/`ADS_ENABLED=false`; флаг добавлен в `.env.example` и может отключаться для тестов/production. Временный поиск до задачи 8 использует переносимые SQLAlchemy-запросы по `public_text`, теме и категории; после последующего решения 2026-06-16 публичными считаются активные официальные материалы темы ЖКХ, включая животные категории внутри нее. SQLite FTS, морфология и ранжирование оставлены для задачи 8. Кнопка "Ответ не подошел" создает `problem_queries` без сырого текста пользователя, со служебным безопасным значением в `anonymized_text`. Проверки: bundled Python pytest - 23 теста проходят; `docker compose build app`; `docker compose run --rm app pytest` - 23 теста проходят; `docker compose up -d --build` и HTTP smoke `/health`, `/`, `/search`, `/legal/disclaimer` возвращают 200.
2026-06-16: Реализована задача 5: добавлена MVP-админка в `app/admin/` с входом одного администратора по `ADMIN_USERNAME`/`ADMIN_PASSWORD`, HMAC-сравнением учетных данных, защищенной signed-cookie сессией без внешней зависимости `itsdangerous`, CSRF-токенами для POST-форм, списком карточек, фильтрами по статусу/теме/категории, карточкой материала, сменой статуса, сменой категории в пределах темы и внутренними заметками. Добавлены шаблоны `app/templates/admin/` и стили в `app/static/styles.css`; публичная выдача продолжает использовать единое правило repository-слоя: только `status=active`, `is_official=true` и публичная тема. Полный сценарий удаления не реализован и остается задачей 10. Проверки: bundled Python pytest - 35 тестов проходят; `docker compose build app`; `docker compose run --rm app pytest` - 35 тестов проходят.
2026-06-16: По решению владельца проекта изменена публичная структура категорий: тема "Животные" больше не показывается отдельным блоком на главной, а все, что касается животных, объединено в один пункт "Животные" внутри блока ЖКХ перед общей категорией "Другое". В `app/db/seed.py` тема `animals` оставлена непубличной для совместимости; детальные категории `stray_dogs`, `animal_capture`, `aggressive_animals`, `shelters`, `pet_rules` скрываются из публичного списка, а существующие материалы/вопросы/проблемные запросы с ними перепривязываются к категории `animals` в теме `housing`. В публичных фильтрах и заголовках выдачи старые детальные животные категории дополнительно сворачиваются в название "Животные", при этом конкретные ответы продолжают находиться по тексту запроса. Публичные тексты теперь говорят только о ЖКХ без отдельного упоминания темы животных в заголовках и подсказках поиска. Обновлены `project-requirements.md`, `task.md` и тесты. Проверки: bundled Python pytest - 36 тестов проходят; `docker compose build app`; `docker compose run --rm app pytest` - 36 тестов проходят.
2026-07-02: Реализована задача 6: добавлен импорт стандартной JSON-выгрузки Telegram через админку `/admin/imports`, фиксированный официальный источник задается через `OFFICIAL_TELEGRAM_SOURCE_ID`, `OFFICIAL_TELEGRAM_SOURCE_NAME`, `OFFICIAL_TELEGRAM_SOURCE_KIND` в конфигурации и `.env.example`. Добавлен модуль `app/importers/telegram_json.py`, шаблоны отчетов импорта в `app/templates/admin/`, скачивание JSON-отчета из `exports/`, сохранение исходного JSON в `imports/`, создание/обновление записи `sources`, `import_batches`, `import_reports` и черновых `materials`. До задачи 8 реальное обезличивание не выполняется: отчет фиксирует `anonymization_status=pending_task_8`, все импортированные материалы остаются `draft` или `needs_review` и не публикуются автоматически. Добавлена зависимость `python-multipart` и тесты `tests/test_telegram_import.py`. Проверки: `docker compose build app`; `docker compose run --rm app pytest tests/test_telegram_import.py` - 9 тестов проходят; `docker compose run --rm app pytest` - 45 тестов проходят.
2026-07-02: Усилено правило секретов: `.env.example` оставлен только как публичный шаблон с явными заглушками без реальных логинов, паролей, токенов и секретных ключей; реальные значения хранятся только в локальном `.env`, который исключен из Git и Docker build context. В `docker-compose.yml` локальный `.env` подключается поверх `.env.example` и не обязателен для свежего клона.
2026-07-02: В `task.md` добавлена отдельная задача 7 "Подготовка к полной приемочной загрузке Telegram JSON": перед полной загрузкой использовать свежую БД или очистить тестовые импортированные черновики после частичной выгрузки. Добавлен безопасный CLI `scripts/cleanup_test_imports.py`: без флага показывает dry-run, с `--execute` удаляет только импортированные материалы в статусах `draft`, `needs_review`, `duplicate` и их служебные зависимые записи; опубликованные материалы, ручные карточки, источники, категории, настройки, отчеты импорта и приватные JSON-файлы не удаляются. Команды запуска добавлены в `README.md`; добавлены тесты сервиса очистки.
2026-07-02: Перед задачей 7 устранен инцидент с приватной Telegram-выгрузкой: папка `ChatExport_2026-07-02` удалена из опубликованного коммита через amend и `git push --force-with-lease`, а в `.gitignore` и `.dockerignore` добавлены правила для `ChatExport*/`, `Telegram Desktop/`, `*.tgs`, `*.webp`, `*.webm`. Реальные Telegram-экспорты нужно хранить вне репозитория, например в `C:\Projects\gorodskoy_private\telegram_exports`, или временно в `imports/`.
2026-07-02: Реализована задача 8: добавлен rule-based модуль `app/services/anonymization.py` для обезличивания телефонов, email, квартир, точных адресов и номеров обращений с сохранением официальных контактов ведомств/служб; ФИО не скрываются автоматически, а создают `person_name_reviews` и переводят карточку в `needs_review`. Импорт Telegram JSON теперь сохраняет неизменный `original_text`, обезличенный `public_text`, `redaction_events`, очередь ФИО, счетчики отчета `anonymization_status=completed`, `redactions_applied`, `person_name_reviews_count` и обезличенную копию JSON в `exports/` с записью пути в `import_batches.anonymized_file_path`. В админке добавлена очередь `/admin/reviews`, а карточка материала показывает скрытые фрагменты и ФИО на проверке без кнопок решений, которые остаются задачей 11. Добавлены тесты `tests/test_anonymization.py` и расширены импортные/админские тесты. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 52 теста проходят.
2026-07-02: Доработана задача 8: расширены правила обезличивания в `app/services/anonymization.py` для личных профилей Telegram/VK/OK и бытовых форматов адресов с домом, корпусом, строением и квартирой; официальный контекст теперь определяется по тексту перед найденным контактом, чтобы следующая фраза про службу не мешала скрытию личных данных. Публичные и юридические тексты обновлены: удалены устаревшие формулировки "до внедрения обезличивания", на главной добавлено предупреждение не вводить ФИО, телефон, номер квартиры и другие персональные данные. Зафиксирована граница задачи: кнопки подтверждения/скрытия ФИО остаются задачей 11. Расширены тесты обезличивания, импорта и публичных страниц. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 56 тестов проходят.
2026-07-02: По решению владельца проекта изменена очередь задач в `task.md` и разделе 28 `project-requirements.md`: сначала завершается браузерное приложение, затем реализуется Telegram-бот. Текущая следующая задача - локальный поиск, словари и проблемные запросы. Telegram-бот перенесен после финальной приемки браузерного приложения, а задача подготовки к полной приемочной загрузке Telegram JSON вынесена последней, чтобы не путать рабочую нумерацию. Старые записи истории с прежними номерами задач не переписывались и остаются историей состояния на дату выполнения.
2026-07-02: `AGENTS.md` разделен на компактный файл правил и обязательный журнал `project-history.md`. В `AGENTS.md` оставлены правила работы, чеклист и список обязательных файлов; вся история изменений перенесена в `project-history.md`, который добавлен в обязательные файлы для чтения перед каждой рабочей сессией. Новые крупные изменения нужно фиксировать в `project-history.md`, а `AGENTS.md` обновлять только при изменении правил, обязательных документов или процесса работы.
2026-07-02: Реализована актуальная задача 8 "Локальный поиск, словари и проблемные запросы": добавлена зависимость `pymorphy3`, модуль `app/search/` с нормализацией русского текста, базовыми словарями ЖКХ и SQLite FTS5-индексом; создана миграция `20260702_0002_search_fts_and_dictionary_candidates` с таблицей `dictionary_candidates` и виртуальной таблицей `search_index`. Публичный сайт переведен на `SearchService`, сохраняет обезличенные проблемные запросы при неуверенной выдаче и показывает предупреждение о неточном совпадении без технических терминов. Добавлена админская страница `/admin/search-quality` для проблемных запросов и кандидатов словаря; подтвержденные варианты формулировок попадают в `question_variants` и переиндексируют материалы, неподтвержденные кандидаты на публичный поиск не влияют. Сценарии смены статуса/категории материала синхронно обновляют поисковый индекс; очистка тестовых импортов учитывает `dictionary_candidates`. Добавлены тесты `tests/test_search.py`, расширены публичные и DB-тесты. Проверки: `docker compose build app`; `docker compose run --rm app pytest tests/test_search.py` - 5 тестов проходят; `docker compose run --rm app pytest` - 61 тест проходит.
2026-07-02: Реализована задача 9 "Защищенное удаление и финальные административные сценарии": добавлены сервисы `app/services/material_deletion.py`, `app/services/person_reviews.py` и `app/services/reprocessing.py`; админка получила двухшаговое удаление карточки через статус `pending_delete`, предпросмотр зависимых записей и финальное подтверждение фразой `УДАЛИТЬ #id`, решения по ФИО сотрудников, повторную обработку карточки и партии импорта без автопубликации, страницу управления категориями `/admin/categories` и кандидаты новых категорий через существующий механизм качества поиска. Расширен `DictionaryCandidateType` значением `category`, добавлена миграция `20260702_0003_dictionary_category_candidates`; подтверждение кандидата категории создает или подтверждает публичную категорию ЖКХ, неподтвержденные кандидаты не влияют на сайт. Добавлены тесты удаления, ФИО, категорий и повторной обработки. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 66 тестов проходят; `docker compose run --rm -e DATABASE_URL=sqlite:////tmp/task9-migration.db app alembic upgrade head` проходит.
2026-07-02: В `AGENTS.md` добавлено правило процесса: после реализации каждого пункта или функционала в итоговом ответе обязательно давать владельцу проекта конкретный алгоритм перезапуска приложения и проверки результата с командами, рабочей папкой, URL и ожидаемыми изменениями.
2026-07-02: Усилено обезличивание персональных обращений в официальных ответах: все, что стоит в начале текста до запятой перед словом "Здравствуйте", теперь скрывается как `salutation_addressee` с заменой `[персональное обращение скрыто]`; также добавлено скрытие голых Telegram/VK-подобных `@username` вне email. Добавлен серверный барьер публикации: карточку нельзя перевести в `active`, если в `public_text` остался не скрытый приветственный адресат или есть нерешенная ФИО-проверка. Для уже загруженной базы добавлены `app/services/material_reanonymization.py` и команда `scripts/reanonymize_materials.py` с dry-run по умолчанию и `--execute` для массового обновления `public_text`, `redaction_events` и поискового индекса из неизменного `original_text`. Команды запуска добавлены в `README.md`; добавлены регрессионные тесты для примеров `@brulik0708`, `Ni T`, `ВАЛЕРИЙ`, `Ирина` перед "Здравствуйте", импорта, массовой переобработки и публикационного барьера.
2026-07-02: Текущая локальная база `data/app.db` переобработана новой командой после создания резервной копии `data/app.before-reanonymize-20260702-221742.db`: контрольный проход показал `unredacted_salutation_prefixes 0`, `redacted_salutation_prefixes 15194`. Полная проверка после изменений: `docker compose build app`; `docker compose run --rm app pytest` - 74 теста проходят.
2026-07-02: Исправлена общая верстка кнопок и верхней области страниц в `app/static/styles.css`: для навигации, строк действий, табличных действий, фильтров и мобильного поиска заданы явные `gap`, сброшены стандартные отступы форм, убраны глобальные мобильные отступы со всех `button`, уменьшены вертикальные отступы шапки и первого контентного блока. Добавлен smoke-тест CSS-контракта в `tests/test_smoke.py`. Проверки: `docker compose run --rm app pytest` - 66 тестов проходят; `docker compose up -d --build`; браузерный обход `/`, `/search`, `/legal/disclaimer`, `/admin/materials`, `/admin/imports`, `/admin/reviews`, `/admin/categories`, `/admin/search-quality`, `/admin/materials/16141`, `/admin/imports/2` показал наличие контента, отсутствие оверлеев ошибок и отсутствие контейнеров кнопок с зазором меньше 8 px.
2026-07-03: В `AGENTS.md` добавлено правило коммуникации для запросов разрешений: сначала в обычном сообщении задавать прямой вопрос и два полных варианта ответа, а во всплывающем системном запросе оставлять только короткий однозначный вопрос, чтобы интерфейс не обрезал варианты.
2026-07-03: Доработано обезличивание приветственных обращений: `salutation_addressee` больше не вставляет заглушку `[персональное обращение скрыто]`, а удаляет весь префикс до слова "Здравствуйте"; `original_fragment` сохраняется в `redaction_events`, `replacement` для таких событий хранится пустой строкой. В админской карточке пустая замена отображается как "удалено" только в шаблоне, без попадания в `public_text`. Текущая база после пересборки образа `docker compose build app` переобработана командой `docker compose run --rm app python scripts/reanonymize_materials.py --execute`: `scanned 16141`, `updated 15194`, `active_moved_to_review 0`. Контроль базы: `old_placeholder_in_public_text 0`, `salutation_events 15194`, `empty_salutation_replacements 15194`, примеры `public_text` начинаются с "Здравствуйте". Полная проверка: `docker compose run --rm app pytest` - 74 теста проходят.
2026-07-03: Реализована задача 10 "Финальная приемка браузерного приложения, тесты и документация запуска" в режиме локальной демонстрации без публичного запуска: финальные юридические реквизиты не предоставлены, поэтому в `README.md` явно зафиксирован блокер публичного запуска до замены `[НАЗВАНИЕ СЕРВИСА]`, `[ВЛАДЕЛЕЦ / ОПЕРАТОР СЕРВИСА]`, `[ДОМЕН СЕРВИСА]`, `[EMAIL ДЛЯ ОБРАЩЕНИЙ]`, `[КОНТАКТЫ ОПЕРАТОРА]`, а также проверки `SERVICE_NAME`, `PUBLIC_BASE_URL`, оператора, контактов и email. README дополнен инструкциями входа в админку, импорта Telegram JSON, ручной smoke-проверки URL, тестирования, повторного обезличивания и очистки тестовых импортов. Добавлены приемочные тесты документации, секретов `.env.example`, `.gitignore`/`.dockerignore`, smoke-маршрутов браузерной части и защиты от попадания комментариев жителей в импортированные публичные материалы. `.dockerignore` больше не исключает несекретные `.gitignore` и `.dockerignore`, чтобы контейнерный pytest мог проверять правила защиты приватных файлов. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 78 тестов проходят.
2026-07-03: По просьбе владельца проекта добавлена последняя задача 13 "Публичный запуск на домене и production-деплой" в `task.md`, а раздел 28 `project-requirements.md` синхронизирован с новой очередью. Задача описывает будущий сценарий, при котором агент сначала запрашивает финальные юридические реквизиты, домен, серверные доступы, DNS/TLS-данные, production-секреты и решение по первичной базе, затем заменяет заглушки, готовит production-конфигурацию без попадания секретов в Git, выполняет тесты, деплой, миграции, HTTPS-настройку, smoke-проверку production-адреса, резервное копирование и процедуру отката. Если обязательных данных или доступов не хватает, запуск должен быть явно заблокирован до их предоставления.
2026-07-03: Добавлена административная доработка "массовая публикация готовых импортированных черновиков": сервис `app/services/bulk_publication.py`, страница админки `/admin/publication`, навигационный пункт "Публикация" и инструкция в `README.md`. Действие работает через dry-run/preview и требует точную фразу подтверждения `ОПУБЛИКОВАТЬ ГОТОВЫЕ ЧЕРНОВИКИ`; публикуются только импортированные официальные черновики публичной темы без нерешенных ФИО и без признаков неочищенного персонального обращения. Ручные черновики, карточки на проверке, непубличные темы и небезопасные тексты не затрагиваются; после публикации перестраивается поисковый индекс. Добавлен тест массовой публикации, CSRF и неверного подтверждения. На текущей базе preview без публикации показал: готово к публикации 15597, на проверке 521, с признаком неочищенного обращения 23. Проверки: `docker compose run --rm app pytest tests/test_admin.py` - 22 теста проходят; `docker compose up -d --build`; `docker compose run --rm app pytest` - 79 тестов проходят.
2026-07-03: Исправлена классификация тем и категорий карточек после выявленного перекоса: транспортные ответы попадали в `Вода` из-за подстроки `вод` в словах `водитель/водителями`, а мусор и часть водных/дворовых материалов оставались в `Другое`. Добавлен общий rule-based классификатор `app/services/classification.py`, который использует нормализацию слов и приоритеты тем: транспорт и благоустройство сохраняются в непубличных темах `Транспорт`/`Благоустройство`, ЖКХ-материалы раскладываются по категориям, добавлена публичная категория ЖКХ `Мусор / отходы`. Импорт Telegram JSON теперь использует общий классификатор и сохраняет данные классификации в `metadata_json`; поисковый словарь дополнен маркерами `waste`. Добавлен сервис и CLI `scripts/reclassify_materials.py` с dry-run по умолчанию, фильтрами `--status`, `--batch-id`, `--limit` и применением через `--execute` с перестроением FTS-индекса. Текущая база перед применением сохранена в `data/app.before-reclassify-20260703.db`; выполнен пересчет активных карточек `docker compose run --rm app python scripts/reclassify_materials.py --status active --execute`: обработано 15597, изменено 7969, повторный dry-run показал `would_change: 0`. Новое распределение активных материалов: ЖКХ/Другое 4943, ЖКХ/Вода 3207, Благоустройство 2457, Транспорт 1833, ЖКХ/Двор 1138, ЖКХ/Отопление 972, ЖКХ/Мусор / отходы 544. Добавлены тесты классификатора, импорта и пересчета. Проверки: `docker compose build app`; `docker compose run --rm app pytest tests/test_classification.py tests/test_telegram_import.py tests/test_database.py` - 21 тест проходит; `docker compose run --rm app pytest` - 83 теста проходят.
2026-07-03: По решению владельца проекта в `task.md` добавлена новая задача 11 "Практические рекомендации из официальных материалов". Цель - сделать публичную выдачу полезнее для вопросов вида "что делать": показывать отдельный справочный блок с действиями, извлеченными из официальных материалов без изменения официального текста и без формирования нового ответа от имени администрации. Задачи Telegram-бота, подготовки к полной загрузке JSON и production-деплоя сдвинуты на номера 12, 13 и 14 соответственно.
2026-07-03: Реализована задача 11 "Практические рекомендации из официальных материалов": добавлена таблица `material_recommendations`, enum `RecommendationType`, миграция `20260703_0004_material_recommendations` и сервис `app/services/recommendations.py` для локального rule-based извлечения действий из `public_text` без внешних AI API. Поисковый FTS-индекс расширен `recommendation_text`, `SearchService` возвращает справочные извлечения и повышает ранг материалов, где действие совпадает с запросом. Публичные страницы `/search` и `/materials/{id}` показывают юридически отделенный блок "Что указано в найденных материалах/материале", а официальный текст остается отдельным неизмененным блоком. Импорт, переобработка материалов, массовая публикация, перестроение поиска, защищенное удаление и очистка тестовых импортов учитывают рекомендации. Добавлена команда `scripts/rebuild_recommendations.py` с dry-run по умолчанию и `--execute`, README дополнен инструкциями. Текущая локальная база после миграции переобработана командой `docker compose run --rm app python scripts/rebuild_recommendations.py --public-only --execute`: обработано 11307 публичных активных материалов, изменено 673 карточки, создано 716 извлечений; smoke `http://127.0.0.1:8000/search?q=Куда%20обращаться%20по%20отоплению%3F` показал блок "Что указано в найденных материалах". Проверки: `docker compose build app`; `docker compose run --rm -e DATABASE_URL=sqlite:////tmp/task11-migration-2.db app alembic upgrade head`; `docker compose run --rm app pytest` - 90 тестов проходят.
2026-07-03: Исправлена регрессия релевантности поиска после задачи 11: запрос "Что делать, если нет горячей воды?" ошибочно поднимал материал про пескосоляную смесь из-за совпадения служебного слова `делать` с формой `делает` и слова `вода`. В `app/search/normalization.py` слова `делать` и `сделать` добавлены в стоп-слова, чтобы фраза "что делать" не участвовала в смысловом ранжировании. Добавлен регрессионный тест, который проверяет, что материал про горячую воду ранжируется выше материала про замерзание воды и пескосоляную смесь. Smoke после перезапуска: `http://127.0.0.1:8000/search?q=Что%20делать,%20если%20нет%20горячей%20воды?` возвращает материалы про горячую воду и не содержит `пескосоляной смесью`. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 91 тест проходит.
2026-07-03: Доработана задача 11 после выявленного плохого ответа "Куда жаловаться на управляющую компанию?": добавлен rule-based слой распознавания потребности пользователя в `app/search/intent.py` (`complaint_escalation`, `service_request`, `emergency`, `billing`, `information`) и классификация практических действий (`self_service`, `oversight`, `emergency_contact`, `operator_contact`, `info_link`). `material_recommendations` получила поле `action_kind` и миграцию `20260703_0005_recommendation_action_kind`; поиск повышает контрольные/надзорные инстанции для жалоб на УК и не показывает в справочном блоке бесполезное действие "обратитесь в управляющую компанию". Импорт Telegram JSON теперь сохраняет обезличенные вопросы жителей в `resident_questions` и связывает официальные ответы с вопросами через `reply_to_message_id` в `material_links`; FTS-индекс получил отдельное поле `question_text`. Для уже загруженной базы добавлены сервис `app/services/question_linking.py` и команда `scripts/rebuild_question_links.py` с dry-run по умолчанию и `--execute` для восстановления связок из сохраненного JSON с последующей перестройкой поиска. README дополнен инструкцией по восстановлению связок. Проверки: `docker compose build app`; `docker compose run --rm -e DATABASE_URL=sqlite:////tmp/task11-intent-migration-fixed.db app alembic upgrade head`; `docker compose run --rm app pytest` - 95 тестов проходят.
2026-07-03: Текущая локальная база `data/app.db` обновлена под доработку задачи 11 после создания резервной копии `data/app.before-task11-intent-20260703-161712.db`: применена миграция `20260703_0005`, выполнена пересборка рекомендаций `docker compose run --rm app python scripts/rebuild_recommendations.py --public-only --execute` (`scanned 11307`, `recommendations 716`) и восстановлены связки вопросов для партии 2 командой `docker compose run --rm app python scripts/rebuild_question_links.py --batch-id 2 --execute` (`scanned 87796`, `questions_created 71655`, `links_created 14889`). После перезапуска `docker compose up -d --build` smoke-запрос `http://127.0.0.1:8000/search?q=Куда%20жаловаться%20на%20управляющую%20компанию%3F` вернул `200`, показал справочный блок с материалом про Госжилинспекцию области и не содержал фразу "обратиться в управляющую компанию" как рекомендацию. Финальная проверка: `docker compose run --rm app pytest` - 95 тестов проходят.
2026-07-03: Доработана публичная поисковая выдача: `SearchItem` теперь содержит вычисляемый `snippet` из `public_text` вокруг совпавшего значимого слова запроса, а шаблон `/search` показывает этот фрагмент вместо первых 320 символов материала. Если совпадение найдено только в категории, связанном вопросе или рекомендации, карточка сохраняет прежний fallback на начало публичного текста; официальный текст в базе не меняется. Добавлены тесты поискового сниппета, fallback-сценария и HTML-выдачи.
2026-07-03: Исправлена системная проблема некорректной выдачи по категориям: классификатор больше не назначает категории по слабым словам без тематического якоря (`агрессивный`, `горячий`, `вывоз`, `детский` и похожие признаки), импорт и массовый пересчет используют публичный очищенный текст материала, а не сырой оригинал, и поиск по выбранной категории требует наличия категорийного доказательства в `public_text`. Для category-only выдачи сниппет строится по маркерам выбранной категории, поэтому смешанные официальные ответы показывают релевантный фрагмент, а не начало чужой темы. Перед пересчетом создана резервная копия `data/app.before-category-reclass-20260703-173731.db`; полный пересчет `scripts/reclassify_materials.py --execute` применился ко всей базе, последующий dry-run по 16141 материалу показал `would_change: 0`. Контроль категории `Животные` показал 85 активных материалов и релевантные сниппеты вокруг animal-фрагментов. Проверки: `docker compose build app`; `docker compose run --rm app pytest` - 101 тест проходит.
